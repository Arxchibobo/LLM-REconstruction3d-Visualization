# Claude Config API key (used for local API authentication)
CLAUDE_CONFIG_API_KEY=dev-only-key
NEXT_PUBLIC_CLAUDE_CONFIG_API_KEY=dev-only-key

# Optional: Override Claude config path (auto-detected from home directory by default)
# NEXT_PUBLIC_CLAUDE_CONFIG_PATH=/path/to/your/.claude
# CLAUDE_CONFIG_PATH=/path/to/your/.claude
